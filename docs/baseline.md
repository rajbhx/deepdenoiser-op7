# Baseline — DeepDenoiser OP7 (r1)

Recorded 2026-08-13 from upstream audit (no local build — CI reproduces this).

## Upstream

- repo: https://github.com/sayampy/deepdenoiser (branch `main`)
- commit: `531b8a8b35b951127b8a31b129a73d3650e0a752` ("Update README.md")
- app version: 1.4.3 · package `com.sayampy.deepdenoiser`

## Build environment (as configured for CI)

- Expo SDK 55 · React Native 0.83.6 · React 19.2.0 · TypeScript ~5.9.3
- onnxruntime-react-native 1.24.3 · model `assets/model/denoiser_model.ort` (13,014,400 B)
- Gradle/AGP: generated by `expo prebuild` from Expo 55 template
- Java 17 (Temurin) · NDK r28b (28.1.13356709) · bun 1.x
- compileSdk 36 · targetSdk 36 · minSdk 24 · ABI arm64-v8a only

## Expected baseline artifacts (populated by first CI run)

| Field | Value |
|---|---|
| upstream commit | 531b8a8b… |
| build command | `bun install --frozen-lockfile && bunx expo prebuild --platform android --no-install && ./gradlew :app:assembleDebug` |
| APK path | `android/app/build/outputs/apk/debug/app-debug.apk` |
| APK SHA-256 | recorded in build metadata (CI) |
| APK size | recorded in build metadata (CI) |
| native libs | `lib/arm64-v8a/` (onnxruntime JNI + RN) |

## On-device baseline (to be filled by manual testing)

Cold-start, import/denoise/export timings, CPU, memory, thermal — see
`docs/benchmarking.md` for the procedure. Until real numbers exist, no
performance claim is made (playbook rule).
